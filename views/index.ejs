<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <% results.forEach(function(result,key){ %>
    <p><%= key+1 %>: <%= result.score %> <%= result.username %></p>
    <% }); %>

    <a href="game">スタート</a>
  </body>
</html>
